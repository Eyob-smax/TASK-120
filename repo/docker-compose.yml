services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "4173:4173"
    environment:
      - NODE_ENV=production

  test:
    build:
      context: ./frontend
      dockerfile: Dockerfile.test
    profiles:
      - test

  e2e:
    build:
      context: ./frontend
      dockerfile: Dockerfile.e2e
    depends_on:
      - frontend
    environment:
      - BASE_URL=http://frontend:4173
    profiles:
      - e2e
